# Vercel Environment Variables

Add to `.env.example`:

```
# Production value is set in the Vercel dashboard (Project → Settings → Environment Variables)
VITE_API_URL=http://localhost:3001
```

In the Vercel dashboard, set `VITE_API_URL` to the Railway server URL for Production and Preview environments.
