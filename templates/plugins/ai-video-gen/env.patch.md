# Append to .env.example (ai-video-gen plugin)

```env
# AI Video Generation (ai-video-gen plugin)
# Kling AI credentials — get from https://klingai.com developer console
KLING_API_KEY=your-kling-api-key
KLING_API_SECRET=your-kling-api-secret

# Kling API endpoint (default is correct for most regions)
# KLING_API_URL=https://api.klingai.com

# Per-user rate limit: max video generations per hour (default: 5)
VIDEO_GEN_RATE_LIMIT_RPH=5
```
